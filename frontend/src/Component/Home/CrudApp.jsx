import React from 'react';
import './CrudApp.css'; // Import the CSS file for styling

const CrudApp = () => {
  return (
    <div className="crud-app">
      <h1 className="rotating-text">CRUD</h1>
    </div>
  );
};

export default CrudApp;
